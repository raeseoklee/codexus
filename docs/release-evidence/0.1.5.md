# 0.1.5 Release Evidence

[Korean](../ko/release-evidence/0.1.5.md)

Status: `0.1.5` stable release candidate

This file is the redacted release evidence artifact for the `0.1.5`
app-instance HTTP evidence probe patch. Do not commit raw run ledgers,
transcripts, stdout dumps, tokens, local paths that reveal private projects, or
model prompts. Record only bounded evidence needed to prove the release
checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.5`
  - Planned tag: `v0.1.5`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.4`:
  - Experimental app-instance HTTP evidence probe:
    `cx app instance evidence probe --instance-id <id> [--url <loopback-url>]`.
  - The probe records bounded, redacted dev-server response evidence for a
    running Codexus-owned worktree app instance.
  - Evidence is linked to an `app-instance-observation` artifact and never
    becomes control, health, cleanup, or completion authority.
  - Non-running instances are recorded as `unavailable` without attempting a
    request.
  - Non-loopback URLs and timeout values above 30 seconds are rejected.
  - Stable JSON contract: no frozen stable fields removed or redefined.
- Local pre-tag release gates:
  - Verification id: `verification_20260603_045740_580400`
  - Result: `passed`
  - Commands: `npm run typecheck`, `node --test tests/app-instance.test.ts`,
    `npm test`, `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`,
    `npm run package:smoke`, and `git diff --check`
  - Release-candidate gate:
    `node codex/skills/codexus/scripts/cx.mjs release check --version 0.1.5 --gate --json`
    passed after this evidence file was added.

## Manual E2E Sign-Off

No new real-model E2E is required before tag publish for `0.1.5`. The new
surface is an experimental loopback HTTP evidence probe over an already
Codexus-owned app instance. It does not start Desktop turns, does not open a
browser, does not claim app health, and does not gain completion authority.

## Post-Publish Evidence

Pending tag-triggered trusted publishing.

## Release Decision

- Planned stable tag: `v0.1.5`
- Planned GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.5`
- Planned npm version: `0.1.5`
- Provenance: trusted publishing
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
