# 0.1.15 Release Evidence

[Korean](../ko/release-evidence/0.1.15.md)

Status: `0.1.15` stable release prep

This file is the redacted release evidence artifact for the roadmap-sequence
hardening patch. Do not commit raw run ledgers, transcripts, stdout dumps,
tokens, local paths that reveal private projects, or model prompts. Record only
bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.15`
  - Planned tag: `v0.1.15`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.14`:
  - Kept the stable `doctor --json` contract aligned with `schemaVersion: 1`.
  - Refreshed roadmap, kanban, project wiki, and stable-contract planning
    projections.
  - Added experimental app-instance screenshot-file evidence binding for
    already captured local screenshot files.
  - Hardened experimental relay implementation-stage gates so approved
    acceptance criteria and concrete local evidence files are required.
  - Added experimental `wiki context --fresh-only --gate` for explicit manual
    context freshness checks. The context remains visible, manual, and
    ineligible for automatic injection.
  - Added manual wiki context to the `cx contract check --target 0.2.0`
    candidate audit without promoting automatic injection.
  - Made deferred evidence tracks explicit in contract readiness: app-instance
    health modeling, automatic context injection, LSP protocol-server lifecycle,
    and plugin always-on supervision.
  - The release does not promote any experimental surface to stable and does
    not redefine frozen stable 0.1.x JSON fields.
  - Action, health, cleanup, prompt-injection, plugin always-on, and completion
    authority remain unpromoted.
- Final pre-tag release gate:
  - Session verification id: `verification_20260608_053144_596975`
  - Command: `npm run release:check`
  - Result: `passed`
  - Additional checks in the same verification:
    `cx contract check --target 0.2.0 --gate --json` and `git diff --check`
    both passed.

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.1.15`. The new runtime
surfaces are local evidence, projection, and gate-hardening commands. They do
not start Codex model work, mutate prompts automatically, own app health or
control, or become completion / verification authority.

## Post-Publish Evidence

Post-publish evidence is intentionally empty before the tag-triggered release.
After publish, record the GitHub Actions run, npm registry version/dist-tags,
GitHub Release URL, live release gate, installed package smoke, and installer
smoke.
