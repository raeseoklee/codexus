# 0.1.4 Release Evidence

[Korean](../ko/release-evidence/0.1.4.md)

Status: `0.1.4` stable release published

This file is the redacted release evidence artifact for the `0.1.4`
app-server-stdio-proof patch. Do not commit raw run ledgers, transcripts,
stdout dumps, tokens, local paths that reveal private projects, or model
prompts. Record only bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep commit:
  - Release commit: `b89d9a07fb89009e5082fbd6f50e24ecde3ad364`
  - Package version: `0.1.4`
  - Tag: `v0.1.4`
  - Publish path: tag-triggered GitHub Actions trusted publishing
  - CI run: `26861746655` (`success`)
  - Pages run: `26861746636` (`success`)
  - Release workflow run: `26861749699` (`success`)
- Release scope since `0.1.3`:
  - Experimental app-server stdio proof harness:
    `cx app-server experiment --stdio-proof --record --json`.
  - The proof harness starts only a fake/Codexus-owned stdio process and
    records a schema-validatable `app-server-stdio-proof` artifact.
  - The artifact proves owner identity, byte/time bounds, transcript exclusion,
    observed method shapes, and no existing Desktop stdio attach.
  - Stable JSON contract: no frozen stable fields removed or redefined.
- Attachment caveat:
  - This release does not enable live Desktop app-server attachment.
  - Existing Desktop stdio pipes remain non-targets.
  - Positive Desktop session mapping remains blocked until a non-disruptive
    observer bridge or explicit user-provided socket produces turn-boundary
    evidence without transcript values.
- Local release gates before tag publish:
  - Verification id: `verification_20260603_032138_b6cc44`
  - Result: `passed`
  - Commands: `npm run release:check`,
    `node src/cli/main.ts release check --version 0.1.4 --gate --json`, and
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Release-candidate gate:
    `node src/cli/main.ts release check --version 0.1.4 --gate --json`
    passed.
- Post-publish live checks:
  - `node src/cli/main.ts release check --version 0.1.4 --live --gate --json`
    passed with `evidenceGaps: []`.
  - `npm view codexus version dist-tags --json --prefer-online` reported
    `version: 0.1.4` and `latest: 0.1.4`.
  - Temporary-prefix `npm install -g codexus@0.1.4` reported `cx --version`
    as `0.1.4` and `codexus schema check --json` passed with 26 schema
    artifacts.
  - GitHub Release `install.sh` installed `0.1.4` with
    `CODEXUS_EXPECTED_VERSION=0.1.4`; `cx schema check --json` passed.

## Manual E2E Sign-Off

No new real-model E2E is required before tag publish for `0.1.4`. The new
surface is an experimental proof harness over a fake/Codexus-owned stdio
process. It does not attach to Desktop stdio, does not start Desktop turns, does
not store transcript values, and does not gain completion authority.

## Release Decision

- Stable tag: `v0.1.4`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.4`
- npm version: `0.1.4`
- `latest` dist-tag: `0.1.4`
- Provenance: published through trusted publishing
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
- Stable blocker: none after local gates, release integrity, trusted
  publishing, live release integrity, and installed-package smoke pass.
