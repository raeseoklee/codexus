# 0.1.7 Release Evidence

[Korean](../ko/release-evidence/0.1.7.md)

Status: `0.1.7` stable release published

This file is the redacted release evidence artifact for the `0.1.7` project LSP
diagnostics and planning-boundary patch. Do not commit raw run ledgers,
transcripts, stdout dumps, tokens, local paths that reveal private projects, or
model prompts. Record only bounded evidence needed to prove the release
checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.7`
  - Planned tag: `v0.1.7`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.6`:
  - Experimental project LSP diagnostics evidence:
    `cx lsp status` detects project diagnostic candidates without starting a
    language server, and `cx lsp check [--gate]` runs explicit project
    diagnostics such as `npm run typecheck`.
  - `npm run lsp:check` is part of `npm run release:check`, and package smoke
    verifies the installed CLI can run `cx lsp check --gate`.
  - Update availability notifications are planned but not implemented in this
    release. The documented first slice is informational, TTL-cached,
    opt-out-capable, and must not fail primary commands or mutate installs.
  - Codex plugin packaging is planned as an experimental
    distribution/discoverability layer. The npm-installed `$codexus` skill
    remains the stable Codex-native adapter, and plugin installation is not
    evidence of always-on supervision.
  - Stable JSON contract: no frozen stable fields removed or redefined.
- Local pre-tag release gates:
  - Initial release check on the pre-bump tree passed:
    `npm run release:check`.
  - Release verification id: `verification_20260603_155045_0afd88`
  - Commands: `npm run release:check`,
    `node src/cli/main.ts release check --version 0.1.7 --gate --json`,
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`,
    and `git diff --check`
  - Result: `passed`
  - Release-candidate gate:
    `node src/cli/main.ts release check --version 0.1.7 --gate --json`
    passed after this evidence file and the version bump were added.

## Manual E2E Sign-Off

No new real-model E2E is required before tag publish for `0.1.7`. The only new
runtime surface is experimental LSP diagnostics evidence over explicit project
commands. It does not start a protocol server, does not edit files, and does not
claim completion authority. Update notification and plugin packaging are
planning-boundary documentation only in this release.

## Post-Publish Evidence

- Release commit: `72469895dda8cf54427918635b9c1ba9d4489fbd`
- GitHub Actions:
  - Release workflow run `26896451371`: `success`
  - CI workflow run `26896446962`: `success`
  - Pages workflow run `26896446888`: `success`
- npm registry:
  - Published version: `0.1.7`
  - `latest` dist-tag: `0.1.7`
  - `next` dist-tag remains on the prerelease channel:
    `0.1.0-alpha.7`
- GitHub Release:
  - URL:
    `https://github.com/raeseoklee/codexus/releases/tag/v0.1.7`
  - Draft: `false`
  - Prerelease: `false`
  - Release asset: `install.sh`
  - `install.sh` SHA-256:
    `e247672a4e4290be90d73ac581b5cb5ea5789bb9a5092c642ad166d5c02cdd87`
- Live release gate:
  - Post-publish verification id: `verification_20260603_155622_918730`
  - Command:
    `node src/cli/main.ts release check --version 0.1.7 --live --gate --json`
  - Result: `passed`
  - Evidence gaps: `[]`
- npm install smoke:
  - Installed `codexus@latest` into a temporary npm prefix.
  - `cx --version` returned `0.1.7`.
  - `codexus schema check --json` passed with `26` schemas and a valid
    app-server fixture.
- Release `install.sh` smoke:
  - Downloaded the `v0.1.7` GitHub Release installer.
  - Ran it with `CODEXUS_EXPECTED_VERSION=0.1.7`,
    `CODEXUS_INSTALL_CODEX_SKILL=0`, and temporary install directories.
  - Installed CLI reported `0.1.7`.
  - `cx schema check --json` passed with `26` schemas and a valid app-server
    fixture.

## Release Decision

- Stable tag: `v0.1.7`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.7`
- npm version: `0.1.7`
- npm `latest`: `0.1.7`
- Provenance: trusted publishing
- Blockers: none observed after post-publish checks
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
