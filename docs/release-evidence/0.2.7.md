# 0.2.7 Release Evidence

[Korean](../ko/release-evidence/0.2.7.md)

Status: `0.2.7` published; npm `next` dist-tag correction pending

This file is the redacted release-prep evidence artifact for the installer
help-safety release-integrity gate. Do not commit raw run ledgers, transcripts,
stdout dumps, tokens, local paths that reveal private projects, or model prompts.
Record only bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.2.7`
  - Planned tag: `v0.2.7`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.2.6`:
  - `release check --gate` now reports
    `releaseIntegrity.installScript.helpNonDestructive:true` only when
    `install.sh` parses `-h`/`--help` and unknown options before Node/npm
    probes, `npm install`, bin-link mutation, or Codex skill adapter
    installation.
  - A regression test proves that an installer without non-destructive help
    handling fails the release gate before publish.
  - Documentation now lists installer help safety as part of local release
    integrity and the release checklist.

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.2.7`. The release is a
release-integrity hardening slice and does not add live model execution, app
health authority, cleanup authority, prompt injection, or new completion
authority.

## Deferred Surfaces

The following remain outside the stable contract:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, leases, and retry;
- LSP protocol-server lifecycle and automatic project LSP application;
- Desktop app-server live attachment;
- automatic context or prompt injection;
- app health authority and automatic cleanup.

## Post-Publish Evidence

- Release workflow run:
  [28341779385](https://github.com/raeseoklee/codexus/actions/runs/28341779385)
  passed.
- npm registry:
  - Published version: `0.2.7`
  - `latest` dist-tag: `0.2.7`
  - `next` dist-tag: `0.2.6` (**pending correction**)
  - Required maintainer command:
    `npm dist-tag add codexus@0.2.7 next`
  - Package shasum: `22f48b6caf07a97f0ae309553c3a02db713847c6`
  - Package integrity:
    `sha512-oWIdudvGJIqfSB49ybmfBRwKcWnjhSEiUNjKmOdgBBuGE1JZz914tplJNxp4wR0WYJrUpJ39U2UhSbuuiVkMzA==`
  - Provenance: npm registry reports SLSA provenance attestation
    `https://slsa.dev/provenance/v1`.
- GitHub Release:
  - URL: <https://github.com/raeseoklee/codexus/releases/tag/v0.2.7>
  - Release asset: `install.sh`
  - `install.sh` SHA-256:
    `05b49aeba8bf2de7c0ac803be309f63ceed94b0c07fe6bb7ac58316e5aa694ed`
- Live release gate:
  - Command:
    `node src/cli/main.ts release check --version 0.2.7 --live --gate --json`
  - Result: failed only on `npm_next_older_than_latest`
  - `nextDistTagAction.command`: `npm dist-tag add codexus@0.2.7 next`
- Installed package smoke:
  - Temporary-prefix `npm install codexus@0.2.7`: passed
  - Installed `codexus --version`: `0.2.7`
  - Installed `codexus schema check --json`: `ok: true`
- GitHub Release installer smoke:
  - Downloaded release `install.sh`: SHA-256 matched the release asset digest
  - Executed release `install.sh --help`: exited `0`, printed usage including
    `CODEXUS_NPM_SPEC`, and created no prefix/bin directories
