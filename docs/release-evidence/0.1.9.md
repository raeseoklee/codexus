# 0.1.9 Release Evidence

[Korean](../ko/release-evidence/0.1.9.md)

Status: `0.1.9` stable release candidate

This file is the redacted release evidence artifact for the `0.1.9` update
availability patch. Do not commit raw run ledgers, transcripts, stdout dumps,
tokens, local paths that reveal private projects, or model prompts. Record only
bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.9`
  - Planned tag: `v0.1.9`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.8`:
  - Added experimental `cx update check --json` for npm `latest`
    update-availability facts through a bounded TTL cache.
  - Added cache-only experimental `update` summaries to `version --json`,
    `doctor --json`, and `session status --json`.
  - Added installed-package smoke coverage for `cx update check` and for the
    non-network update summary attached to `version` and `doctor`.
  - The release does not promote any experimental surface to stable and does
    not redefine frozen stable 0.1.x JSON fields. The top-level `update` field
    is additive on stable outputs, and the nested update report remains
    informational.
- Local pre-tag release gates:
  - Release verification id: `verification_20260605_003055_949daa`
  - Command: `npm run release:check`
  - Result: `passed`
  - Release-candidate integrity gate:
    `node src/cli/main.ts release check --version 0.1.9 --gate --json`
  - Release-candidate integrity result: `passed`
  - Release-candidate integrity evidence gaps: `[]`
  - Repository knowledge gate:
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Repository knowledge result: `passed`
  - Update check smoke:
    `CODEXUS_NO_UPDATE_CHECK=1 node src/cli/main.ts update check --json`
  - Update check result: `disabled` by env, `registryChecked: false`,
    `installationMutated: false`
  - `git diff --check`: `passed`

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.1.9`. The new runtime
surface is an experimental advisory update-availability report. It does not
start Codex model work, edit files, install packages, mutate the local
installation, or become completion / verification / release authority.

## Post-Publish Evidence

Pending until `v0.1.9` tag publish completes.

## Release Decision

- Stable tag: `v0.1.9`
- npm version: pending
- npm `latest`: pending
- Provenance: trusted publishing
- Blockers: none observed in local pre-tag gates
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
