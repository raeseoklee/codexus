# 0.1.0 Release Evidence

[Korean](../ko/release-evidence/0.1.0.md)

Status: pending

This file is the redacted manual sign-off artifact for the `0.1.0` stable cut.
Do not commit raw run ledgers, transcripts, stdout dumps, tokens, local paths
that reveal private projects, or model prompts. Record only bounded evidence
needed to prove the release checklist.

## Automated Evidence

- `npm run release:check`: pending
- `npm run package:smoke`: pending
- `cx supply-chain check --gate --json`: pending
- GitHub CI for release commit: pending
- Trusted-publishing prerelease rehearsal: pending

## Manual E2E Sign-Off

- Real `codex-exec` run against a throwaway fixture:
  - Date:
  - Ledger id:
  - Verification command:
  - Outcome:
  - Redacted notes:
- `$codexus` skill inside Codex CLI/TUI:
  - Date:
  - Commands exercised:
  - Outcome:
  - Redacted notes:
- Real `resume` and `cancel`:
  - Date:
  - Ledger ids:
  - Outcome:
  - Redacted notes:
- Notify hook `turn-ended` observed:
  - Date:
  - `capabilities.hooks` transition:
  - Redacted notes:

## Release Decision

- Stable tag: pending
- npm version: pending
- `latest` dist-tag: pending
- `next` dist-tag: pending
- Known deferred surfaces confirmed in docs/output: pending
