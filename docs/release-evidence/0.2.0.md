# 0.2.0 Release Evidence

[Korean](../ko/release-evidence/0.2.0.md)

Status: `0.2.0` stable release prep

This file is the redacted release evidence artifact for the first
stable-contract promotion milestone. Do not commit raw run ledgers, transcripts,
stdout dumps, tokens, local paths that reveal private projects, or model
prompts. Record only bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.2.0`
  - Planned tag: `v0.2.0`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.15`:
  - Promoted `cx architecture check --gate --json` to stable for the narrow
    declared-policy `forbidden-import` contract only.
  - Promoted `cx wiki context --topic <name> --fresh-only --gate --json` to
    stable for explicit manual context selection and local freshness gating
    only.
  - Kept broad architecture layering, type-aware graph claims, automatic
    context injection, wiki context approval artifacts, and action/control
    surfaces outside the stable contract.
  - Updated JSON contract, release policy, implementation status, remaining
    work, roadmap kanban, project wiki, README, and the 0.2 readiness plan.
  - Added installed-package smoke coverage for promoted architecture output and
    both fresh and stale `wiki context --fresh-only --gate` branches.
- Final pre-tag release gate:
  - Session verification id: `verification_20260608_134710_a589e6`
  - Command: `npm run release:check`
  - Result: `passed`
  - Additional checks in the same verification:
    `cx contract check --target 0.2.0 --gate --json` and `git diff --check`
    both passed.

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.2.0`. The release
promotes local, deterministic evidence surfaces and does not add live model
execution, automatic prompt injection, app health authority, or plugin
always-on supervision.

## Deferred Surfaces

The following remain outside the `0.2.0` stable contract:

- live app-instance health authority and automatic cleanup;
- live `cx autopilot run`;
- active relay engine spawning;
- Desktop app-server live attachment;
- automatic context or prompt injection;
- LSP protocol-server lifecycle and automatic project LSP application;
- plugin always-on supervision.

## Post-Publish Evidence

Pending tag-triggered trusted publishing.
