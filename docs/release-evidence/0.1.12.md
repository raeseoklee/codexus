# 0.1.12 Release Evidence

[Korean](../ko/release-evidence/0.1.12.md)

Status: `0.1.12` stable release prep

This file is the redacted release evidence artifact for the executable release
cadence policy patch. Do not commit raw run ledgers, transcripts, stdout dumps,
tokens, local paths that reveal private projects, or model prompts. Record only
bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.12`
  - Planned tag: `v0.1.12`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.11`:
  - Added `cx release policy --json` and `cx release policy --gate --json`.
  - Added English and Korean release policy docs for the "small commits, larger
    releases" cadence, hotfix exceptions, stable-contract version boundary, and
    release evidence rules.
  - Wired `release:policy` into `npm run release:check` so missing policy docs
    block release prep before tag publish.
  - Updated public release docs, JSON contract notes, implementation status,
    remaining work, changelog, and Codexus skill references.
  - The release does not promote any experimental surface to stable and does
    not redefine frozen stable 0.1.x JSON fields.
- Local pre-tag release gates:
  - Session checkpoint id: `checkpoint_20260606_035456_b5c179`
  - Session verification id: `verification_20260606_035456_69b615`
  - Command: `npm run release:check`
  - Result: `passed`
  - Focused release policy tests:
    `node --test tests/release-policy.test.ts tests/release-integrity.test.ts`
  - Focused release policy tests result: `passed`
  - Release policy gate:
    `node src/cli/main.ts release policy --gate --json`
  - Release policy result: `passed`
  - Repo knowledge gate:
    `node src/cli/main.ts repo check --gate --json`
  - Repo knowledge result: `passed`

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.1.12`. The new runtime
surface reports release cadence policy and blocks only missing policy documents.
It does not start Codex model work, edit files beyond the release prep changes,
install packages, mutate local installation, or become completion / verification
authority.

## Post-Publish Evidence

Pending tag-triggered publish.

