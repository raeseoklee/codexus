# 0.1.6 Release Evidence

[Korean](../ko/release-evidence/0.1.6.md)

Status: `0.1.6` stable release candidate

This file is the redacted release evidence artifact for the `0.1.6`
app-instance log snapshot evidence patch. Do not commit raw run ledgers,
transcripts, stdout dumps, tokens, local paths that reveal private projects, or
model prompts. Record only bounded evidence needed to prove the release
checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.6`
  - Planned tag: `v0.1.6`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.5`:
  - Experimental app-instance log snapshot evidence adapter:
    `cx app instance evidence logs --instance-id <id> [--tail <n>]`.
  - The adapter records bounded, redacted stdout/stderr tail evidence for a
    Codexus-owned worktree app instance.
  - Evidence is linked to an `app-instance-observation` artifact and never
    becomes control, health, cleanup, or completion authority.
  - Snapshot output redacts secret-shaped log lines before writing the evidence
    artifact.
  - Stable JSON contract: no frozen stable fields removed or redefined.
- Local pre-tag release gates:
  - Feature verification id: `verification_20260603_120410_a00f19`
  - Result: `passed`
  - Commands: `npm run typecheck`, `node --test tests/app-instance.test.ts`,
    `npm test`, `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`,
    and `git diff --check`
  - Release verification id: `verification_20260603_120614_f07862`
  - Release-candidate gate:
    `node codex/skills/codexus/scripts/cx.mjs release check --version 0.1.6 --gate --json`
    passed after this evidence file was added.

## Manual E2E Sign-Off

No new real-model E2E is required before tag publish for `0.1.6`. The new
surface is an experimental log evidence adapter over an already Codexus-owned
app instance. It does not start Desktop turns, does not open a browser, does not
claim app health, and does not gain completion authority.

## Post-Publish Evidence

Pending tag-triggered trusted publishing.

## Release Decision

- Planned stable tag: `v0.1.6`
- Planned GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.6`
- Planned npm version: `0.1.6`
- Provenance: trusted publishing
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
