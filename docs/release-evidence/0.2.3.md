# 0.2.3 Release Evidence

[Korean](../ko/release-evidence/0.2.3.md)

Status: `0.2.3` prepared; post-publish evidence pending

This file is the redacted release-prep evidence artifact for the advisory
update-notice, app-evidence summary, wiki-approval listing, and README demo
patch. Do not commit raw run ledgers, transcripts, stdout dumps, tokens, local
paths that reveal private projects, or model prompts. Record only bounded
evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.2.3`
  - Planned tag: `v0.2.3`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.2.2`:
  - Non-JSON `version`, `doctor`, `session status`, and `session hud` now show
    cache-only advisory update notices only when fresh cached npm evidence
    proves a newer Codexus version exists.
  - Update summaries now include a structured `update.notification` object so
    the Codex-native `$codexus` skill can surface available updates in chat
    without mutating installation.
  - `cx app instance evidence summary --json` reports bounded observation
    rollups for Codexus-owned app instances without claiming health, control,
    cleanup, or completion authority.
  - `cx wiki context approvals --json` lists manual-only wiki context approvals
    without becoming automatic prompt injection or a completion authority.
  - README now includes a redacted VHS demo and reproducible tape source. The
    demo is not a live model transcript.
  - Feature reference, implementation status, project wiki, roadmap kanban,
    Codex-native adapter docs, and release evidence docs were refreshed before
    tag publish.
- Final pre-tag release gate:
  - Session verification id: `verification_20260615_101331_c8a82d`
  - Commands:
    - `npm run release:check`
    - `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
    - `git diff --check`
  - Result: `passed before commit`
  - Release-prep repair note: the first pre-tag run failed because
    `codex/plugins/codexus/.codex-plugin/plugin.json` still declared `0.2.2`
    after the package version bump. The manifest was updated to `0.2.3`, and
    the final verification above passed.

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.2.3`. The release adds
cache-only advisory update notices, bounded evidence summaries, explicit wiki
approval listings, and documentation/demo artifacts. It does not add live model
execution, automatic installation mutation, automatic prompt injection, app
health authority, cleanup authority, or new completion authority.

## Deferred Surfaces

The following remain outside the stable contract:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, leases, and retry;
- LSP protocol-server lifecycle and automatic project LSP application;
- Desktop app-server live attachment;
- automatic context or prompt injection;
- app health authority and automatic cleanup.

## Post-Publish Evidence

Pending until tag-triggered trusted publishing completes.
