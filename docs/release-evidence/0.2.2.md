# 0.2.2 Release Evidence

[Korean](../ko/release-evidence/0.2.2.md)

Status: `0.2.2` pre-tag release gate passed

This file is the redacted release-prep evidence artifact for the
release/update-channel hardening patch. Do not commit raw run ledgers,
transcripts, stdout dumps, tokens, local paths that reveal private projects, or
model prompts. Record only bounded evidence needed to prove the release
checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.2.2`
  - Planned tag: `v0.2.2`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.2.1`:
  - Tag-triggered trusted publishing now runs
    `npm run publish:stable -- --no-dist-tag-sync`, so the OIDC
    trusted-publisher surface is limited to `npm publish`.
  - `cx release check --gate --json` now gates the trusted-publishing workflow
    against post-publish npm dist-tag mutation and reports
    `trustedPublishSkipsDistTagMutation:true` when the workflow is wired
    correctly.
  - The post-publish live sign-off remains responsible for proving npm
    `next >= latest`; if `next` trails `latest`, an authenticated maintainer
    corrects it before release completion is declared.
  - Release policy, public release checklist, JSON contract, implementation
    status, project wiki release management, changelog, and release evidence
    docs were refreshed before tag publish.
- Final pre-tag release gate:
  - Session verification id: `verification_20260612_095745_aa483f`
  - Commands:
    - `npm run release:check`
    - `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
    - `git diff --check`
  - Result: `passed before commit`

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.2.2`. The release
changes release workflow policy, static release-integrity evidence, tests, and
documentation. It does not add live model execution, app control authority,
automatic prompt injection, or new completion authority.

## Deferred Surfaces

The following remain outside the stable contract:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, leases, and retry;
- LSP protocol-server lifecycle and automatic project LSP application;
- Desktop app-server live attachment;
- automatic context or prompt injection;
- app health authority and automatic cleanup.

## Post-Publish Evidence

Pending tag-triggered publish.
