# 0.1.13 Release Evidence

[Korean](../ko/release-evidence/0.1.13.md)

Status: `0.1.13` stable release prep

This file is the redacted release evidence artifact for the five-surface
evidence projection patch. Do not commit raw run ledgers, transcripts, stdout
dumps, tokens, local paths that reveal private projects, or model prompts.
Record only bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.13`
  - Planned tag: `v0.1.13`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.12`:
  - Added experimental `cx session tasks list/add/update/complete/block`.
  - Added experimental `cx repo graph import/search/explain`.
  - Added experimental `cx wiki build --mode advisory`.
  - Added experimental `cx app instance evidence metrics`.
  - Added experimental `cx cron recovery` and `cx gateway recovery`.
  - Updated English/Korean design docs, implementation status, remaining work,
    roadmap kanban, schema registry, command references, and release notes.
  - The release does not promote any experimental surface to stable and does
    not redefine frozen stable 0.1.x JSON fields.
  - New task, graph, wiki, metric, and automation recovery outputs are
    projections only; they do not claim completion, health, scheduler, retry,
    cleanup, or prompt-injection authority.
- Pre-bump E2E gate:
  - Session verification id: `verification_20260607_065727_98734a`
  - Command: `npm run release:check`
  - Result: `passed`
- Final pre-tag release gate:
  - Session verification id: `verification_20260607_070049_a84337`
  - Command: `npm run release:check`
  - Result: `passed`

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.1.13`. The new runtime
surfaces are local projection and evidence commands. They do not start Codex
model work, mutate prompts automatically, own unattended scheduler dispatch,
retry failed automation automatically, or become completion / verification
authority.

## Post-Publish Evidence

To be filled after tag-triggered trusted publishing completes:

- Release commit: `pending`
- GitHub Actions:
  - Release workflow run: `pending`
  - CI workflow run: `pending`
  - Pages workflow run: `pending`
- npm registry:
  - Published version: `pending`
  - `latest` dist-tag: `pending`
  - `next` dist-tag: `pending`
  - Provenance predicate type: `pending`
  - Package file count: `pending`
  - Package shasum: `pending`
- GitHub Release:
  - URL: `pending`
  - Draft: `pending`
  - Prerelease: `pending`
  - Release asset: `install.sh`
  - `install.sh` SHA-256: `pending`
- Live release gate:
  - Command:
    `node src/cli/main.ts release check --version 0.1.13 --live --gate --json`
  - Result: `pending`
- Installed package smoke:
  - Command:
    `npm install -g --prefix <tmp> codexus@latest`
  - Installed `cx --version`: `pending`
  - Installed `cx session tasks list --json`: `pending`
  - Installed `cx repo graph search --help`: `pending`
  - Installed `cx wiki build --help`: `pending`
  - Installed `cx schema check --json`: `pending`
- GitHub Release installer smoke:
  - Command:
    `curl -fsSL https://github.com/raeseoklee/codexus/releases/download/v0.1.13/install.sh | CODEXUS_EXPECTED_VERSION=0.1.13 CODEXUS_INSTALL_CODEX_SKILL=0 ... sh`
  - Installed `cx --version`: `pending`
  - Installed `cx schema check --json`: `pending`
