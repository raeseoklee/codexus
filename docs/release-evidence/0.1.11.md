# 0.1.11 Release Evidence

[Korean](../ko/release-evidence/0.1.11.md)

Status: `0.1.11` stable release candidate

This file is the redacted release evidence artifact for the `0.1.11`
prerelease-update opt-in patch. Do not commit raw run ledgers, transcripts,
stdout dumps, tokens, local paths that reveal private projects, or model prompts.
Record only bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.1.11`
  - Planned tag: `v0.1.11`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.10`:
  - Added explicit `cx update check --channel next --json` support for npm
    `next` dist-tag checks.
  - Kept stable update checks on the npm `latest` dist-tag by default.
  - Added separate `latest.json` / `next.json` update caches so stable and
    prerelease advice cannot mix silently.
  - Added structured errors for invalid or missing update channel values.
  - Updated the Codex skill command reference and English/Korean documentation
    to mark prerelease checks as explicit opt-in.
  - Updated the packaged Codex plugin manifest version to match the package
    version.
  - The release does not promote any experimental surface to stable and does
    not redefine frozen stable 0.1.x JSON fields.
- Local pre-tag release gates:
  - Session verification id: `verification_20260605_165418_616926`
  - Command: `npm run ci`
  - Result: `passed`
  - Full release check:
    `npm run release:check`
  - Full release check result: `passed`
  - Focused update regression tests:
    `node --test tests/update.test.ts`
  - Focused update regression result: `passed`
  - Package smoke:
    `npm run package:smoke`
  - Package smoke result: `passed`
  - Release-candidate integrity gate:
    `node src/cli/main.ts release check --version 0.1.11 --gate --json`
  - Release-candidate integrity result: `passed`
  - Release-candidate integrity evidence gaps: `[]`

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.1.11`. The new runtime
surface is an explicit advisory update lookup for the npm `next` dist-tag. It
does not start Codex model work, edit files, install packages, mutate the local
installation, or become completion / verification / release authority.

## Post-Publish Evidence

Pending tag-triggered trusted publishing.

## Release Decision

- Stable tag: `v0.1.11` planned
- npm version: `0.1.11` planned
- Provenance: trusted publishing planned
- Blockers: none observed in local pre-tag gates
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
