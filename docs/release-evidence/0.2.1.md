# 0.2.1 Release Evidence

[Korean](../ko/release-evidence/0.2.1.md)

Status: `0.2.1` release prep pending tag publish

This file is the redacted release-prep evidence artifact for the action-readiness
evidence bundle. Do not commit raw run ledgers, transcripts, stdout dumps,
tokens, local paths that reveal private projects, or model prompts. Record only
bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep:
  - Package version: `0.2.1`
  - Planned tag: `v0.2.1`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.2.0`:
  - Added experimental `cx lsp adapters --json` and
    `lsp-adapter.schema.json` so Codexus can report diagnostics-command support
    while keeping protocol-server lifecycle, edit, workspace-mutation, and
    completion authority false.
  - Added experimental `cx autopilot run-gate --policy <path> --json` and
    `autopilot-run-gate.schema.json` so pre-run contract, approval, scope, and
    fresh-evidence readiness can be checked without starting a live autopilot
    run.
  - Added `automation-scheduler-readiness-v1` report-only evidence to
    cron/gateway status and recovery output, listing durable queue, lease,
    retry-policy, approval/policy, and recovery-proof gaps while keeping
    scheduler, retry, cleanup, health, and completion authority false.
  - Added `sessionProjection` to app-server observer status so recorded
    observer evidence can be cited in the session evidence loop without proving
    live Desktop attachment or gaining completion authority.
  - Updated README, feature docs, design docs, implementation status, remaining
    work, roadmap kanban, project wiki, Codex skill docs, package smoke, and
    schema/package policy coverage.
- Final pre-tag release gate:
  - Session verification id: `verification_20260611_035657_c29a2a`
  - Command: `npm run release:check`
  - Result: `passed before commit`
  - Additional checks:
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json` and
    `git diff --check` passed before commit.

## Manual E2E Sign-Off

No real-model E2E is required before tag publish for `0.2.1`. The release adds
local, deterministic readiness and adapter-reporting evidence surfaces. It does
not add live autopilot execution, automatic scheduler ownership, LSP
protocol-server lifecycle, live Desktop attachment, automatic prompt injection,
or new completion authority.

## Deferred Surfaces

The following remain outside the stable contract:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, leases, and retry;
- LSP protocol-server lifecycle and automatic project LSP application;
- Desktop app-server live attachment;
- automatic context or prompt injection;
- app health authority and automatic cleanup.

## Post-Publish Evidence

Fill this section after tag-triggered publishing:

- Release commit: `<pending>`
- GitHub Actions:
  - Release workflow run `<pending>`: `<pending>`
- npm registry:
  - Published version: `<pending>`
  - `latest` dist-tag: `<pending>`
  - `next` dist-tag: `<pending>`
- GitHub Release:
  - URL: `<pending>`
  - Release asset: `install.sh`
- Live release gate:
  - Command: `node src/cli/main.ts release check --version 0.2.1 --live --gate --json`
  - Result: `<pending>`
- Installed package smoke:
  - Command: `npm install -g --prefix <tmp> codexus@latest`
  - Installed `cx --version`: `<pending>`
