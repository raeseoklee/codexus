# 0.1.3 Release Evidence

[Korean](../ko/release-evidence/0.1.3.md)

Status: `0.1.3` stable release published

This file is the redacted release evidence artifact for the `0.1.3`
experimental-action-surface patch. Do not commit raw run ledgers, transcripts,
stdout dumps, tokens, local paths that reveal private projects, or model
prompts. Record only bounded evidence needed to prove the release checklist.

## Automated Evidence

- Release prep commit:
  - Release commit: `d98ed4c93f1e181b5d91d5c384d207c852b11642`
  - Package version: `0.1.3`
  - Tag: `v0.1.3`
  - Publish path: tag-triggered GitHub Actions trusted publishing
  - CI run: `26823115338` (`success`)
  - Pages run: `26823115343` (`success`)
  - Release workflow run: `26823221811` (`success`)
- Release scope since `0.1.2`:
  - Experimental action surfaces: worktree app instance launcher start/stop,
    explicit-approval automation live dispatch, and app-instance observation
    binding.
  - Experimental evidence surfaces: autopilot contract foundation, compiled
    wiki deterministic slice, operational control first slice, and relay
    acceptance-criteria verification-matrix enforcement.
  - App instance status now reports explicit stale/orphan lifecycle policy,
    heartbeat age, stale thresholds, cleanup policy, stop policy, and
    non-authority guarantees.
  - Stable JSON contract: no frozen stable fields removed or redefined.
- Action-surface caveat:
  - This release adds experimental action surfaces. They remain gated and are
    not part of the frozen stable 0.1.x JSON contract.
  - These action surfaces take effect only behind owner-identity verification or
    explicit approval, and Codexus never auto-cleans or claims
    completion/health authority over them.
- Local release gates before tag publish:
  - Verification id: `verification_20260602_130318_525ba1`
  - Result: `passed`
  - Commands: `npm test`, `npm run package:smoke`, and
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Release-candidate gate after the release commit:
    `node src/cli/main.ts release check --version 0.1.3 --gate --json`
    passed.
- Post-publish live checks:
  - `node src/cli/main.ts release check --version 0.1.3 --live --gate --json`
    passed with `evidenceGaps: []`.
  - `npm view codexus version dist-tags --json --prefer-online` reported
    `version: 0.1.3` and `latest: 0.1.3`.
  - Temporary-prefix `npm install -g codexus@0.1.3` reported `cx --version`
    as `0.1.3` and `codexus schema check --json` passed.
  - GitHub Release `install.sh` installed `0.1.3` with
    `CODEXUS_EXPECTED_VERSION=0.1.3`; `cx schema check --json` passed.

## Manual E2E Sign-Off

No new real-model E2E is required before tag publish for `0.1.3`. The new
surfaces are experimental and gated: app instance stop requires owner identity
verification, automation live dispatch requires explicit approval, and app
instance lifecycle projection does not gain cleanup, health, or completion
authority. The `0.1.0` manual E2E remains the real Codex execution baseline.

## Release Decision

- Stable tag: `v0.1.3`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.3`
- npm version: `0.1.3`
- `latest` dist-tag: `0.1.3`
- Provenance: published through trusted publishing
- Known deferred surfaces remain gated and self-report as experimental or
  deferred.
- Stable blocker: none after local gates, release integrity, trusted
  publishing, live release integrity, and installed-package smoke pass.
