# 0.2.1 Release Evidence

[English](../../release-evidence/0.2.1.md)

Status: `0.2.1` release prep pending tag publish

이 파일은 action-readiness evidence bundle을 위한 redacted release-prep evidence
artifact입니다. Raw run ledger, transcript, stdout dump, token, private project가
드러나는 local path, model prompt를 커밋하지 않습니다. Release checklist를 증명하는 데
필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.2.1`
  - Planned tag: `v0.2.1`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.2.0`:
  - Experimental `cx lsp adapters --json`와 `lsp-adapter.schema.json`을
    추가했습니다. Codexus는 diagnostics-command support를 보고하되
    protocol-server lifecycle, edit, workspace-mutation, completion authority를
    false로 유지합니다.
  - Experimental `cx autopilot run-gate --policy <path> --json`와
    `autopilot-run-gate.schema.json`을 추가했습니다. Live autopilot run을
    시작하지 않고 pre-run contract, approval, scope, fresh-evidence readiness를
    확인할 수 있습니다.
  - Cron/gateway status와 recovery output에 `automation-scheduler-readiness-v1`
    report-only evidence를 추가했습니다. Durable queue, lease, retry-policy,
    approval/policy, recovery-proof gap을 나열하면서 scheduler, retry, cleanup,
    health, completion authority를 false로 유지합니다.
  - App-server observer status에 `sessionProjection`을 추가했습니다. 기록된
    observer evidence를 session evidence loop에서 인용할 수 있지만 live Desktop
    attachment proof나 completion authority가 되지는 않습니다.
  - README, feature docs, design docs, implementation status, remaining work,
    roadmap kanban, project wiki, Codex skill docs, package smoke, schema/package
    policy coverage를 갱신했습니다.
- Final pre-tag release gate:
  - Session verification id: `verification_20260611_035657_c29a2a`
  - Command: `npm run release:check`
  - Result: `passed before commit`
  - Additional checks:
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`와
    `git diff --check`가 commit 전 통과했습니다.

## Manual E2E Sign-Off

`0.2.1` tag publish 전 real-model E2E는 필요하지 않습니다. 이 release는 local,
deterministic readiness와 adapter-reporting evidence surface를 추가합니다. Live
autopilot execution, automatic scheduler ownership, LSP protocol-server lifecycle,
live Desktop attachment, automatic prompt injection, 새로운 completion authority는
추가하지 않습니다.

## Deferred Surfaces

다음 항목은 stable contract 밖에 남아 있습니다:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, lease, retry;
- LSP protocol-server lifecycle과 자동 project LSP 적용;
- Desktop app-server live attachment;
- automatic context 또는 prompt injection;
- app health authority와 automatic cleanup.

## Post-Publish Evidence

Tag-triggered publishing 이후 이 섹션을 채웁니다:

- Release commit: `<pending>`
- GitHub Actions:
  - Release workflow run `<pending>`: `<pending>`
- npm registry:
  - Published version: `<pending>`
  - `latest` dist-tag: `<pending>`
  - `next` dist-tag: `<pending>`
- GitHub Release:
  - URL: `<pending>`
  - Release asset: `install.sh`
- Live release gate:
  - Command: `node src/cli/main.ts release check --version 0.2.1 --live --gate --json`
  - Result: `<pending>`
- Installed package smoke:
  - Command: `npm install -g --prefix <tmp> codexus@latest`
  - Installed `cx --version`: `<pending>`
