# 0.2.2 Release Evidence

[English](../../release-evidence/0.2.2.md)

Status: `0.2.2` pre-tag release gate passed

이 파일은 release/update-channel hardening patch를 위한 redacted release-prep
evidence artifact입니다. Raw run ledger, transcript, stdout dump, token, private
project가 드러나는 local path, model prompt를 커밋하지 않습니다. Release checklist를
증명하는 데 필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.2.2`
  - Planned tag: `v0.2.2`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.2.1`:
  - Tag-triggered trusted publishing은 이제
    `npm run publish:stable -- --no-dist-tag-sync`를 실행하므로 OIDC
    trusted-publisher 권한 표면이 `npm publish`로 제한됩니다.
  - `cx release check --gate --json`은 이제 trusted-publishing workflow의
    post-publish npm dist-tag mutation을 gate하고, workflow가 올바르게 배선되면
    `trustedPublishSkipsDistTagMutation:true`를 보고합니다.
  - Post-publish live sign-off는 계속 npm `next >= latest` 증명을 담당합니다.
    `next`가 `latest`보다 뒤처지면 release 완료 선언 전에 인증된 maintainer가 이를
    보정합니다.
  - Tag publish 전에 release policy, public release checklist, JSON contract,
    implementation status, project wiki release management, changelog, release
    evidence docs를 갱신했습니다.
- Final pre-tag release gate:
  - Session verification id: `verification_20260612_095745_aa483f`
  - Commands:
    - `npm run release:check`
    - `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
    - `git diff --check`
  - Result: `passed before commit`

## Manual E2E Sign-Off

`0.2.2` tag publish 전 real-model E2E는 필요하지 않습니다. 이 release는 release
workflow policy, static release-integrity evidence, tests, documentation을 변경합니다.
Live model execution, app control authority, automatic prompt injection, 새로운
completion authority는 추가하지 않습니다.

## Deferred Surfaces

다음 항목은 stable contract 밖에 남아 있습니다:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, lease, retry;
- LSP protocol-server lifecycle과 자동 project LSP 적용;
- Desktop app-server live attachment;
- automatic context 또는 prompt injection;
- app health authority와 automatic cleanup.

## Post-Publish Evidence

Tag-triggered publish 대기 중입니다.
