# 0.1.6 Release Evidence

[English](../../release-evidence/0.1.6.md)

상태: `0.1.6` stable release candidate

이 파일은 `0.1.6` app-instance log snapshot evidence patch를 위한 redacted release
evidence artifact입니다. Raw run ledger, transcript, stdout dump, token, private
project를 드러내는 local path, model prompt는 커밋하지 않습니다. Release checklist를
증명하는 데 필요한 bounded evidence만 기록합니다.

## 자동 증거

- Release prep:
  - Package version: `0.1.6`
  - Planned tag: `v0.1.6`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- `0.1.5` 이후 release scope:
  - Experimental app-instance log snapshot evidence adapter:
    `cx app instance evidence logs --instance-id <id> [--tail <n>]`.
  - Adapter는 Codexus-owned worktree app instance의 stdout/stderr tail evidence를
    bounded, redacted artifact로 기록합니다.
  - Evidence는 `app-instance-observation` artifact에 연결되지만 control, health,
    cleanup, completion authority가 되지 않습니다.
  - Snapshot output은 evidence artifact를 쓰기 전에 secret-shaped log line을
    redact합니다.
  - Stable JSON contract: frozen stable field 제거/재정의 없음.
- Tag publish 전 local release gate:
  - Feature verification id: `verification_20260603_120410_a00f19`
  - Result: `passed`
  - Commands: `npm run typecheck`, `node --test tests/app-instance.test.ts`,
    `npm test`, `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`,
    `git diff --check`
  - Release verification id: `verification_20260603_120614_f07862`
  - Release-candidate gate:
    `node codex/skills/codexus/scripts/cx.mjs release check --version 0.1.6 --gate --json`
    은 이 evidence 파일 추가 후 통과했습니다.

## 수동 E2E Sign-Off

`0.1.6` tag publish 전 새 real-model E2E는 필요하지 않습니다. 새 surface는 이미
Codexus-owned인 app instance 위의 experimental log evidence adapter입니다. Desktop
turn을 시작하지 않고, browser를 열지 않고, app health를 주장하지 않으며, completion
authority를 얻지 않습니다.

## Post-Publish Evidence

Tag-triggered trusted publishing 대기 중.

## Release Decision

- Planned stable tag: `v0.1.6`
- Planned GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.6`
- Planned npm version: `0.1.6`
- Provenance: trusted publishing
- 알려진 deferred surface는 계속 gate 뒤에 있으며 experimental/deferred로
  자기보고합니다.
