# 0.2.6 Release Evidence

[English](../../release-evidence/0.2.6.md)

Status: `0.2.6` prepared; post-publish live sign-off pending

이 파일은 installer help-safety hotfix를 위한 redacted release-prep evidence
artifact입니다. Raw run ledger, transcript, stdout dump, token, private project
local path, model prompt를 커밋하지 마세요. Release checklist를 증명하는 데 필요한
bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.2.6`
  - Planned tag: `v0.2.6`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- `0.2.5` 이후 release scope:
  - `install.sh --help`와 `install.sh -h`는 이제 Node/npm probe, npm install,
    filesystem write, bin-link mutation, Codex skill adapter 설치보다 먼저 usage를
    출력합니다.
  - 알 수 없는 installer argument는 설치로 fall-through하지 않고 exit code `2`로
    fail closed 합니다.
  - README 설치 안내는 `sh install.sh --help`로 installer usage를 먼저 확인할 수
    있음을 안내합니다.

## Manual E2E Sign-Off

`0.2.6` tag publish 전 real-model E2E는 요구하지 않습니다. 이 release는 installer
safety hotfix이며 live model execution, app health authority, cleanup authority,
prompt injection, 새로운 completion authority를 추가하지 않습니다.

## Deferred Surfaces

다음 항목은 stable contract 밖에 남아 있습니다:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, lease, retry;
- LSP protocol-server lifecycle과 automatic project LSP application;
- Desktop app-server live attachment;
- automatic context 또는 prompt injection;
- app health authority와 automatic cleanup.

## Post-Publish Evidence

`v0.2.6` publish와 live sign-off 실행 전까지 pending입니다:

- Release workflow run: pending
- npm registry:
  - Published version: pending
  - `latest` dist-tag: pending
  - `next` dist-tag: pending
  - Package shasum: pending
  - Package integrity: pending
  - Provenance: pending
- GitHub Release:
  - URL: pending
  - Release asset: `install.sh` pending
  - `install.sh` SHA-256: pending
- Live release gate:
  - Command:
    `node src/cli/main.ts release check --version 0.2.6 --live --gate --json`
  - Result: pending
  - Evidence gaps: pending
- Installed package smoke: pending
- GitHub Release installer smoke: pending
