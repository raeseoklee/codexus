# 0.1.11 Release Evidence

[English](../../release-evidence/0.1.11.md)

Status: `0.1.11` stable release published

이 파일은 `0.1.11` prerelease-update opt-in patch의 redacted release evidence
artifact입니다. Raw run ledger, transcript, stdout dump, token, private project가
드러나는 local path, model prompt를 커밋하지 않습니다. Release checklist를 증명하는 데
필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.1.11`
  - Planned tag: `v0.1.11`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.10`:
  - npm `next` dist-tag check를 위한 명시적
    `cx update check --channel next --json` 지원을 추가했습니다.
  - Stable update check는 계속 기본적으로 npm `latest` dist-tag만 봅니다.
  - Stable advice와 prerelease advice가 조용히 섞이지 않도록 `latest.json` /
    `next.json` update cache를 분리했습니다.
  - 잘못되거나 누락된 update channel 값에 대한 structured error를 추가했습니다.
  - Codex skill command reference와 영문/한국어 문서를 갱신해 prerelease check가 명시적
    opt-in임을 표시했습니다.
  - Packaged Codex plugin manifest version을 package version과 맞췄습니다.
  - 이 release는 어떤 experimental surface도 stable로 승격하지 않고, frozen stable
    0.1.x JSON field를 재정의하지 않습니다.
- Local pre-tag release gates:
  - Session verification id: `verification_20260605_165418_616926`
  - Command: `npm run ci`
  - Result: `passed`
  - Full release check:
    `npm run release:check`
  - Full release check result: `passed`
  - Focused update regression tests:
    `node --test tests/update.test.ts`
  - Focused update regression result: `passed`
  - Package smoke:
    `npm run package:smoke`
  - Package smoke result: `passed`
  - Release-candidate integrity gate:
    `node src/cli/main.ts release check --version 0.1.11 --gate --json`
  - Release-candidate integrity result: `passed`
  - Release-candidate integrity evidence gaps: `[]`

## Manual E2E Sign-Off

`0.1.11` tag publish 전 real-model E2E는 필요하지 않습니다. 새 runtime surface는 npm
`next` dist-tag를 위한 명시적 advisory update lookup입니다. 이 기능은 Codex model work를
시작하지 않고, 파일을 수정하지 않고, package를 설치하지 않고, local installation을 변경하지
않으며, completion / verification / release authority가 되지 않습니다.

## Post-Publish Evidence

- Release commit: `f8c5d8e278fa2bc4226b7134dae6115d5143412a`
- GitHub Actions:
  - Release workflow run `27028482352`: `success`
  - CI workflow run `27028481595`: `success`
  - Pages workflow run `27028481585`: `success`
- npm registry:
  - Published version: `0.1.11`
  - `latest` dist-tag: `0.1.11`
  - `next` dist-tag는 prerelease channel에 남아 있습니다:
    `0.1.0-alpha.7`
  - Provenance predicate type: `https://slsa.dev/provenance/v1`
  - Package file count: `46`
- GitHub Release:
  - URL:
    `https://github.com/raeseoklee/codexus/releases/tag/v0.1.11`
  - Draft: `false`
  - Prerelease: `false`
  - Release asset: `install.sh`
  - `install.sh` SHA-256:
    `e247672a4e4290be90d73ac581b5cb5ea5789bb9a5092c642ad166d5c02cdd87`
- Live release gate:
  - Command:
    `node src/cli/main.ts release check --version 0.1.11 --live --gate --json`
  - Result: `passed`
- npm install smoke:
  - Temporary npm prefix에 `codexus@latest`를 설치했습니다.
  - `cx --version`은 `0.1.11`을 반환했습니다.
  - `cx schema check --json`은 `26`개 schema와 valid app-server fixture로
    통과했습니다.
  - `CODEXUS_NO_UPDATE_CHECK=1 cx update check --channel next --json`은
    `channel: "next"`, `distTag: "next"`, `registryChecked: false`를 보고했습니다.
- Release `install.sh` smoke:
  - `v0.1.11` GitHub Release installer를 다운로드했습니다.
  - `CODEXUS_EXPECTED_VERSION=0.1.11`, `CODEXUS_INSTALL_CODEX_SKILL=0`, temporary
    `CODEXUS_NPM_PREFIX`, temporary `CODEXUS_BIN_DIR`로 실행했습니다.
  - 설치된 CLI는 `0.1.11`을 보고했습니다.
  - `cx schema check --json`은 `26`개 schema와 valid app-server fixture로 통과했습니다.

## Release Decision

- Stable tag: `v0.1.11`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.11`
- npm version: `0.1.11`
- npm `latest`: `0.1.11`
- Provenance: trusted publishing
- Blockers: pre-tag 또는 post-publish check에서 관측된 blocker 없음
- Known deferred surface는 계속 gated 상태이며 experimental 또는 deferred로 self-report합니다.
