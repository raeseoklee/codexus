# 0.1.10 Release Evidence

[English](../../release-evidence/0.1.10.md)

Status: `0.1.10` stable release candidate

이 파일은 `0.1.10` Codex plugin-packaging diagnostics patch의 redacted release
evidence artifact입니다. Raw run ledger, transcript, stdout dump, token, private
project가 드러나는 local path, model prompt를 커밋하지 않습니다. Release checklist를
증명하는 데 필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.1.10`
  - Planned tag: `v0.1.10`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.9`:
  - Packaged Codex plugin manifest, skill, wrapper diagnostic을 위한 experimental
    `cx plugin status --json`을 추가했습니다.
  - `codex/plugins/codexus` 아래 experimental Codex plugin package를 추가했습니다.
  - Plugin package를 doctor와 package-smoke가 검증합니다.
  - npm-installed `$codexus` skill은 계속 stable Codex-native adapter입니다.
  - Codex가 문서화된 plugin install-location contract를 제공하기 전까지 installed-plugin
    상태는 `deferred`입니다.
  - Plugin packaging은 always-on supervision, heartbeat evidence, health authority,
    completion authority를 주장하지 않습니다.
  - 이 release는 어떤 experimental surface도 stable로 승격하지 않고, frozen stable
    0.1.x JSON field를 재정의하지 않습니다.
- Local pre-tag release gates:
  - Release verification id: `verification_20260605_070256_2dbe73`
  - Command: `npm run release:check`
  - Result: `passed`
  - Release-candidate integrity gate:
    `node src/cli/main.ts release check --version 0.1.10 --gate --json`
  - Release-candidate integrity result: `passed`
  - Release-candidate integrity evidence gaps: `[]`
  - Repository knowledge gate:
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Repository knowledge result: `passed`
  - Plugin package validation:
    `validate_plugin.py codex/plugins/codexus`
  - Plugin package validation result: `passed`

## Manual E2E Sign-Off

`0.1.10` tag publish 전 real-model E2E는 필요하지 않습니다. 새 runtime surface는
experimental package diagnostic과 plugin distribution artifact입니다. 이 기능은 Codex model
work를 시작하지 않고, 파일을 수정하지 않고, package를 설치하지 않고, live Codex plugin
runtime에 attach하지 않으며, local installation을 변경하지 않고, completion / verification /
release authority가 되지 않습니다.

## Post-Publish Evidence

Tag publish 대기 중입니다.

## Release Decision

- Stable tag: `pending`
- GitHub Release: `pending`
- npm version: `pending`
- npm `latest`: `pending`
- Provenance: trusted publishing expected
- Blockers: pre-tag local check에서 관측된 blocker 없음
- Known deferred surface는 계속 gated 상태이며 experimental 또는 deferred로 self-report합니다.
