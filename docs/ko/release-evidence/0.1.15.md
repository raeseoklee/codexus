# 0.1.15 Release Evidence

[English](../../release-evidence/0.1.15.md)

Status: `0.1.15` stable release prep

이 파일은 roadmap-sequence hardening patch의 redacted release evidence artifact입니다.
Raw run ledger, transcript, stdout dump, token, private project가 드러나는 local path,
model prompt를 커밋하지 않습니다. Release checklist를 증명하는 데 필요한 bounded
evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.1.15`
  - Planned tag: `v0.1.15`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.14`:
  - Stable `doctor --json` contract가 `schemaVersion: 1`과 정렬되도록 유지했습니다.
  - Roadmap, kanban, project wiki, stable-contract planning projection을 갱신했습니다.
  - 이미 캡처된 local screenshot file을 app instance에 연결하는 experimental
    app-instance screenshot-file evidence를 추가했습니다.
  - Experimental relay implementation-stage gate를 강화해 승인된 acceptance criteria와
    구체적인 local evidence file을 요구하도록 했습니다.
  - 명시적 manual context freshness check를 위한 experimental
    `wiki context --fresh-only --gate`를 추가했습니다. Context는 계속 visible/manual이며
    automatic injection 대상이 아닙니다.
  - Automatic injection을 승격하지 않고 manual wiki context를
    `cx contract check --target 0.2.0` candidate audit에 추가했습니다.
  - Contract readiness에서 app-instance health modeling, automatic context injection,
    LSP protocol-server lifecycle, plugin always-on supervision 같은 deferred evidence
    track을 명시했습니다.
  - 이 release는 어떤 experimental surface도 stable로 승격하지 않고, frozen stable
    0.1.x JSON field를 재정의하지 않습니다.
  - Action, health, cleanup, prompt-injection, plugin always-on, completion authority는
    승격하지 않습니다.
- Final pre-tag release gate:
  - Session verification id: `verification_20260608_053144_596975`
  - Command: `npm run release:check`
  - Result: `passed`
  - 같은 verification에서 추가로 실행한
    `cx contract check --target 0.2.0 --gate --json`과 `git diff --check`도
    통과했습니다.

## Manual E2E Sign-Off

`0.1.15` tag publish 전 real-model E2E는 필요하지 않습니다. 새 runtime surface들은
local evidence, projection, gate-hardening command입니다. Codex model work를 시작하지
않고, prompt를 자동 변경하지 않고, app health/control을 소유하지 않으며, completion /
verification authority가 되지 않습니다.

## Post-Publish Evidence

Post-publish evidence는 tag-triggered release 전에는 의도적으로 비워 둡니다. Publish
후 GitHub Actions run, npm registry version/dist-tag, GitHub Release URL, live release
gate, installed package smoke, installer smoke를 기록합니다.
