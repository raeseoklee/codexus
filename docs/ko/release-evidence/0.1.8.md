# 0.1.8 Release Evidence

[English](../../release-evidence/0.1.8.md)

Status: `0.1.8` stable release published

이 파일은 `0.1.8` contract promotion readiness audit patch의 redacted release
evidence artifact입니다. Raw run ledger, transcript, stdout dump, token, private
project가 드러나는 local path, model prompt를 커밋하지 않습니다. Release checklist를
증명하는 데 필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.1.8`
  - Planned tag: `v0.1.8`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.7`:
  - 문서화된 0.2.0 contract rule을 위한 structured promotion readiness audit인
    experimental `cx contract check --target 0.2.0 --json`을 추가했습니다.
  - `cx contract check --target 0.2.0 --gate --json`은 audit된 promotion candidate가
    모두 non-stable 또는 unfrozen인 동안 의도적으로 실패합니다.
  - 이 release는 어떤 experimental surface도 stable로 승격하지 않으며, frozen stable
    0.1.x JSON field를 재정의하지 않습니다.
  - Action surface는 0.2.0 promotion readiness에서 계속 deferred로 남습니다.
- Local pre-tag release gates:
  - Release verification id: `verification_20260603_235019_8d8d72`
  - Command: `npm run release:check`
  - Result: `passed`
  - Release-candidate integrity gate:
    `node src/cli/main.ts release check --version 0.1.8 --gate --json`
  - Release-candidate integrity result: `passed`
  - Release-candidate integrity evidence gaps: `[]`
  - Repository knowledge gate:
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Repository knowledge result: `passed`
  - Contract readiness audit:
    `node src/cli/main.ts contract check --target 0.2.0 --json`
  - Contract readiness result: `not_ready`, expected
    `stable_promotion_missing` evidence gap 포함. 이 release에서 stable 0.2.0
    promotion을 의도하지 않았으므로 `0.1.8` blocker가 아닙니다.
  - `git diff --check`: `passed`

## Manual E2E Sign-Off

`0.1.8` tag publish 전 새로운 real-model E2E는 필요하지 않습니다. 새 runtime surface는
experimental local contract-readiness audit뿐입니다. 이 명령은 process를 시작하지 않고,
파일을 수정하지 않으며, remote service를 호출하지 않고, `0.2.0` 준비 완료를 주장하지
않습니다.

## Post-Publish Evidence

- Release commit: `aaa406794bf6ad9ca986c696bdb709c45b3f9c22`
- GitHub Actions:
  - Release workflow run `26920616876`: `success`
  - CI workflow run `26920611508`: `success`
  - Pages workflow run `26920611497`: `success`
- npm registry:
  - Published version: `0.1.8`
  - `latest` dist-tag: `0.1.8`
  - `next` dist-tag는 prerelease channel에 남아 있습니다:
    `0.1.0-alpha.7`
- GitHub Release:
  - URL:
    `https://github.com/raeseoklee/codexus/releases/tag/v0.1.8`
  - Draft: `false`
  - Prerelease: `false`
  - Release asset: `install.sh`
  - `install.sh` SHA-256:
    `e247672a4e4290be90d73ac581b5cb5ea5789bb9a5092c642ad166d5c02cdd87`
- Live release gate:
  - Post-publish verification id: `verification_20260603_235740_e37a79`
  - Command:
    `node src/cli/main.ts release check --version 0.1.8 --live --gate --json`
  - Result: `passed`
  - Evidence gaps: `[]`
- npm install smoke:
  - Temporary npm prefix에 `codexus@latest`를 설치했습니다.
  - `cx --version`은 `0.1.8`을 반환했습니다.
  - `codexus schema check --json`은 `26`개 schema와 valid app-server fixture로
    통과했습니다.
- Release `install.sh` smoke:
  - `v0.1.8` GitHub Release installer를 다운로드했습니다.
  - `CODEXUS_EXPECTED_VERSION=0.1.8`, `CODEXUS_INSTALL_CODEX_SKILL=0`, temporary
    install directory로 실행했습니다.
  - 설치된 CLI는 `0.1.8`을 보고했습니다.
  - `cx schema check --json`은 `26`개 schema와 valid app-server fixture로
    통과했습니다.

## Release Decision

- Stable tag: `v0.1.8`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.8`
- npm version: `0.1.8`
- npm `latest`: `0.1.8`
- Provenance: trusted publishing
- Blockers: post-publish check 이후 관측된 blocker 없음
- Known deferred surface는 계속 gated 상태이며 experimental 또는 deferred로 self-report합니다.
