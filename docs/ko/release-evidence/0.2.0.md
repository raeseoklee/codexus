# 0.2.0 Release Evidence

[English](../../release-evidence/0.2.0.md)

Status: `0.2.0` stable release published

이 파일은 첫 stable-contract promotion milestone을 위한 redacted release evidence
artifact입니다. Raw run ledger, transcript, stdout dump, token, private project가
드러나는 local path, model prompt를 커밋하지 않습니다. Release checklist를 증명하는 데
필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.2.0`
  - Published tag: `v0.2.0`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.15`:
  - `cx architecture check --gate --json`을 좁은 declared-policy
    `forbidden-import` contract에 한해 stable로 승격했습니다.
  - `cx wiki context --topic <name> --fresh-only --gate --json`을 명시적 manual
    context selection과 local freshness gate에 한해 stable로 승격했습니다.
  - Broad architecture layering, type-aware graph claim, automatic context
    injection, wiki context approval artifact, action/control surface는 stable
    contract 밖에 유지했습니다.
  - JSON contract, release policy, implementation status, remaining work, roadmap
    kanban, project wiki, README, 0.2 readiness plan을 갱신했습니다.
  - 승격된 architecture output과 `wiki context --fresh-only --gate`의 fresh/stale
    branch를 installed-package smoke에 추가했습니다.
- Final pre-tag release gate:
  - Session verification id: `verification_20260608_134710_a589e6`
  - Command: `npm run release:check`
  - Result: `passed`
  - 같은 verification에서 추가로 실행한
    `cx contract check --target 0.2.0 --gate --json`과 `git diff --check`도
    통과했습니다.

## Manual E2E Sign-Off

`0.2.0` tag publish 전 real-model E2E는 필요하지 않습니다. 이 release는 local,
deterministic evidence surface를 승격하며 live model execution, automatic prompt
injection, app health authority, plugin always-on supervision을 추가하지 않습니다.

## Deferred Surfaces

다음 항목은 `0.2.0` stable contract 밖에 남아 있습니다:

- live app-instance health authority와 automatic cleanup;
- live `cx autopilot run`;
- active relay engine spawning;
- Desktop app-server live attachment;
- automatic context 또는 prompt injection;
- LSP protocol-server lifecycle과 자동 project LSP 적용;
- plugin always-on supervision.

## Post-Publish Evidence

- Release commit: `a92da53b123c37e1abf2954719f16f31316e251b`
- GitHub Actions:
  - Release workflow run `27142251722`: `success`
- npm registry:
  - Published version: `0.2.0`
  - `latest` dist-tag: `0.2.0`
  - `next` dist-tag는 prerelease channel `0.1.0-alpha.7`에 유지됩니다.
  - Package file count: `50`
  - Package shasum: `b492c9dcb88739c7f91f7bfeaf0f2d17d6fe5fe1`
  - Package integrity:
    `sha512-sf2dA57yaDCYy296JpkYujcOhT/DRuYczRppbw75B9O23CsGkYs0QJnI8eHsR/xQFwT6k3FJYStgqh/sSdOdpQ==`
- GitHub Release:
  - URL: `https://github.com/raeseoklee/codexus/releases/tag/v0.2.0`
  - Draft: `false`
  - Prerelease: `false`
  - Release asset: `install.sh`
  - `install.sh` SHA-256:
    `e247672a4e4290be90d73ac581b5cb5ea5789bb9a5092c642ad166d5c02cdd87`
- Live release gate:
  - Command: `node src/cli/main.ts release check --version 0.2.0 --live --gate --json`
  - Result: `passed`
  - Evidence gaps: `[]`
- Installed package smoke:
  - Command: `npm install -g --prefix <tmp> codexus@latest`
  - Installed `cx --version`: `0.2.0`
  - Installed `cx schema check --json`: `ok: true`
- GitHub Release installer smoke:
  - Command:
    `curl -fsSL https://github.com/raeseoklee/codexus/releases/download/v0.2.0/install.sh | CODEXUS_EXPECTED_VERSION=0.2.0 CODEXUS_INSTALL_CODEX_SKILL=0 CODEXUS_BIN_DIR=<tmp>/bin CODEXUS_NPM_PREFIX=<tmp>/npm-global sh`
  - Installed `cx --version`: `0.2.0`
  - Installed `cx schema check --json`: `ok: true`
