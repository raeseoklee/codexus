# 0.1.3 Release Evidence

[English](../../release-evidence/0.1.3.md)

상태: `0.1.3` stable release 배포 완료

이 파일은 `0.1.3` experimental-action-surface patch를 위한 redacted release evidence
artifact입니다. Raw run ledger, transcript, stdout dump, token, private project를
드러내는 local path, model prompt는 커밋하지 않습니다. Release checklist를 증명하는
데 필요한 bounded evidence만 기록합니다.

## 자동 증거

- Release prep commit:
  - Release commit: `d98ed4c93f1e181b5d91d5c384d207c852b11642`
  - Package version: `0.1.3`
  - Tag: `v0.1.3`
  - Publish path: tag-triggered GitHub Actions trusted publishing
  - CI run: `26823115338` (`success`)
  - Pages run: `26823115343` (`success`)
  - Release workflow run: `26823221811` (`success`)
- `0.1.2` 이후 release scope:
  - Experimental action surface: worktree app instance launcher start/stop,
    explicit-approval automation live dispatch, app-instance observation
    binding.
  - Experimental evidence surface: autopilot contract foundation, compiled
    wiki deterministic slice, operational control first slice, relay
    acceptance-criteria verification-matrix enforcement.
  - App instance status는 이제 명시적 stale/orphan lifecycle policy, heartbeat
    age, stale threshold, cleanup policy, stop policy, non-authority 보장을
    보고합니다.
  - Stable JSON contract: frozen stable field 제거/재정의 없음.
- Action-surface caveat:
  - 이 release는 experimental action surface를 추가합니다. 이 surface들은 계속
    gate 뒤에 있으며 frozen stable 0.1.x JSON contract에는 포함되지 않습니다.
  - 이 action surface는 owner-identity verification 또는 explicit approval 뒤에서만
    효과를 갖고, Codexus는 이를 자동 cleanup하거나 completion/health authority로
    주장하지 않습니다.
- Tag publish 전 local release gate:
  - Verification id: `verification_20260602_130318_525ba1`
  - Result: `passed`
  - Commands: `npm test`, `npm run package:smoke`,
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Release commit 이후 release-candidate gate:
    `node src/cli/main.ts release check --version 0.1.3 --gate --json`
    통과.
- Post-publish live check:
  - `node src/cli/main.ts release check --version 0.1.3 --live --gate --json`
    이 `evidenceGaps: []`로 통과.
  - `npm view codexus version dist-tags --json --prefer-online` 결과
    `version: 0.1.3`, `latest: 0.1.3`.
  - 임시 prefix에서 `npm install -g codexus@0.1.3` 실행 후
    `cx --version`은 `0.1.3`, `codexus schema check --json` 통과.
  - GitHub Release `install.sh`를 `CODEXUS_EXPECTED_VERSION=0.1.3`으로
    실행해 `0.1.3` 설치 확인, `cx schema check --json` 통과.

## 수동 E2E Sign-Off

`0.1.3` tag publish 전 새 real-model E2E는 필요하지 않습니다. 새 surface는
experimental이며 gate 뒤에 있습니다. App instance stop은 owner identity verification을
요구하고, automation live dispatch는 explicit approval을 요구하며, app instance
lifecycle projection은 cleanup, health, completion authority를 얻지 않습니다.
`0.1.0` manual E2E가 실제 Codex 실행 baseline으로 남아 있습니다.

## Release Decision

- Stable tag: `v0.1.3`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.3`
- npm version: `0.1.3`
- `latest` dist-tag: `0.1.3`
- Provenance: trusted publishing으로 배포됨
- 알려진 deferred surface는 계속 gate 뒤에 있으며 experimental/deferred로
  자기보고합니다.
- Stable blocker: local gate, release integrity, trusted publishing, live release
  integrity, installed-package smoke 통과 후 없음.
