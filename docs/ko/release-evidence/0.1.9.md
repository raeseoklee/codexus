# 0.1.9 Release Evidence

[English](../../release-evidence/0.1.9.md)

Status: `0.1.9` stable release candidate

이 파일은 `0.1.9` update availability patch의 redacted release evidence artifact입니다.
Raw run ledger, transcript, stdout dump, token, private project가 드러나는 local path,
model prompt를 커밋하지 않습니다. Release checklist를 증명하는 데 필요한 bounded
evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.1.9`
  - Planned tag: `v0.1.9`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- Release scope since `0.1.8`:
  - Bounded TTL cache를 통해 npm `latest` update-availability fact를 보고하는
    experimental `cx update check --json`을 추가했습니다.
  - `version --json`, `doctor --json`, `session status --json`에 cache-only
    experimental `update` summary를 추가했습니다.
  - `cx update check`, 그리고 `version`/`doctor`에 붙는 non-network update summary를
    installed-package smoke가 검증합니다.
  - 이 release는 어떤 experimental surface도 stable로 승격하지 않고, frozen stable
    0.1.x JSON field를 재정의하지 않습니다. Stable output의 top-level `update` field는
    additive이며, nested update report는 informational 상태로 남습니다.
- Local pre-tag release gates:
  - Release verification id: `verification_20260605_003055_949daa`
  - Command: `npm run release:check`
  - Result: `passed`
  - Release-candidate integrity gate:
    `node src/cli/main.ts release check --version 0.1.9 --gate --json`
  - Release-candidate integrity result: `passed`
  - Release-candidate integrity evidence gaps: `[]`
  - Repository knowledge gate:
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Repository knowledge result: `passed`
  - Update check smoke:
    `CODEXUS_NO_UPDATE_CHECK=1 node src/cli/main.ts update check --json`
  - Update check result: env로 `disabled`, `registryChecked: false`,
    `installationMutated: false`
  - `git diff --check`: `passed`

## Manual E2E Sign-Off

`0.1.9` tag publish 전 real-model E2E는 필요하지 않습니다. 새 runtime surface는
experimental advisory update-availability report입니다. 이 기능은 Codex model work를
시작하지 않고, 파일을 수정하지 않고, package를 설치하지 않으며, local installation을
변경하지 않고, completion / verification / release authority가 되지 않습니다.

## Post-Publish Evidence

`v0.1.9` tag publish 완료 후 기록합니다.

## Release Decision

- Stable tag: `v0.1.9`
- npm version: pending
- npm `latest`: pending
- Provenance: trusted publishing
- Blockers: local pre-tag gate에서 관측된 blocker 없음
- Known deferred surface는 계속 gated 상태이며 experimental 또는 deferred로 self-report합니다.
