# 0.1.4 Release Evidence

[English](../../release-evidence/0.1.4.md)

상태: `0.1.4` stable release 배포 완료

이 파일은 `0.1.4` app-server-stdio-proof patch를 위한 redacted release evidence
artifact입니다. Raw run ledger, transcript, stdout dump, token, private project를
드러내는 local path, model prompt는 커밋하지 않습니다. Release checklist를 증명하는
데 필요한 bounded evidence만 기록합니다.

## 자동 증거

- Release prep commit:
  - Release commit: `b89d9a07fb89009e5082fbd6f50e24ecde3ad364`
  - Package version: `0.1.4`
  - Tag: `v0.1.4`
  - Publish path: tag-triggered GitHub Actions trusted publishing
  - CI run: `26861746655` (`success`)
  - Pages run: `26861746636` (`success`)
  - Release workflow run: `26861749699` (`success`)
- `0.1.3` 이후 release scope:
  - Experimental app-server stdio proof harness:
    `cx app-server experiment --stdio-proof --record --json`.
  - Proof harness는 fake/Codexus-owned stdio process만 시작하고
    schema-validatable `app-server-stdio-proof` artifact를 기록합니다.
  - Artifact는 owner identity, byte/time bound, transcript exclusion, observed
    method shape, 기존 Desktop stdio attach 금지를 증명합니다.
  - Stable JSON contract: frozen stable field 제거/재정의 없음.
- Attachment caveat:
  - 이 release는 live Desktop app-server attachment를 활성화하지 않습니다.
  - 기존 Desktop stdio pipe는 계속 non-target입니다.
  - Positive Desktop session mapping은 non-disruptive observer bridge 또는 explicit
    user-provided socket이 transcript 값 없는 turn-boundary evidence를 만들 때까지
    blocked입니다.
- Tag publish 전 local release gate:
  - Verification id: `verification_20260603_032138_b6cc44`
  - Result: `passed`
  - Commands: `npm run release:check`,
    `node src/cli/main.ts release check --version 0.1.4 --gate --json`,
    `node codex/skills/codexus/scripts/cx.mjs repo check --gate --json`
  - Release-candidate gate:
    `node src/cli/main.ts release check --version 0.1.4 --gate --json`
    통과.
- Post-publish live check:
  - `node src/cli/main.ts release check --version 0.1.4 --live --gate --json`
    이 `evidenceGaps: []`로 통과.
  - `npm view codexus version dist-tags --json --prefer-online` 결과
    `version: 0.1.4`, `latest: 0.1.4`.
  - 임시 prefix에서 `npm install -g codexus@0.1.4` 실행 후
    `cx --version`은 `0.1.4`, `codexus schema check --json`은 26개 schema
    artifact로 통과.
  - GitHub Release `install.sh`를 `CODEXUS_EXPECTED_VERSION=0.1.4`로
    실행해 `0.1.4` 설치 확인, `cx schema check --json` 통과.

## 수동 E2E Sign-Off

`0.1.4` tag publish 전 새 real-model E2E는 필요하지 않습니다. 새 surface는
fake/Codexus-owned stdio process 위의 experimental proof harness입니다. Desktop stdio에
attach하지 않고, Desktop turn을 시작하지 않고, transcript 값을 저장하지 않으며,
completion authority를 얻지 않습니다.

## Release Decision

- Stable tag: `v0.1.4`
- GitHub Release:
  `https://github.com/raeseoklee/codexus/releases/tag/v0.1.4`
- npm version: `0.1.4`
- `latest` dist-tag: `0.1.4`
- Provenance: trusted publishing으로 배포됨
- 알려진 deferred surface는 계속 gate 뒤에 있으며 experimental/deferred로
  자기보고합니다.
- Stable blocker: local gate, release integrity, trusted publishing, live release
  integrity, installed-package smoke 통과 후 없음.
