# 0.1.0 Release Evidence

[English](../../release-evidence/0.1.0.md)

상태: prerelease evidence 수집 완료; stable은 npm trusted-publisher 설정에서 blocked

이 파일은 `0.1.0` stable cut을 위한 redacted manual sign-off artifact입니다. Raw run
ledger, transcript, stdout dump, token, private project를 드러내는 local path, model
prompt는 커밋하지 않습니다. Release checklist를 증명하는 데 필요한 bounded evidence만
기록합니다.

## 자동 증거

- `npm run release:check`: `0.1.0-alpha.5`에서 local 통과
  - 155 unit tests 통과
  - `npm run package:smoke` 통과
  - supply-chain report는 evidence gap 0 / blocking unknown 0
- `npm run package:smoke`: `0.1.0-alpha.5`에서 local 통과
- `cx supply-chain check --gate --json`: local 통과(`gate.status: "passed"`,
  `supplyChain.status: "pass"`)
- prerelease rehearsal commit의 GitHub CI: 통과
  - Commit: `872e2be`
  - Run: `26706393655`
  - Jobs: Typecheck and test, Node 22 compatibility
- trusted-publishing prerelease rehearsal: blocked
  - Workflow run `26706413143`은 release check까지 통과했지만 `npm publish`에서
    registry `E404` / OIDC publish 권한 없음으로 실패.
  - `npm trust github codexus --repo raeseoklee/codexus --file release.yml`는
    `raeseoklee`로 인증됐지만 `E403`으로 실패했고, npm은 trust 작업에 2FA가
    필요하다고 보고했습니다.
  - Fallback token publish는 `codexus@0.1.0-alpha.5`로 성공.
  - Fallback 이후 npm dist-tag: `latest = 0.1.0-alpha.5`, `next =
    0.1.0-alpha.5`.

## 수동 E2E Sign-Off

- throwaway fixture에서 실제 `codex-exec` run:
  - 날짜: 2026-05-31T07:26Z-2026-05-31T07:27Z
  - Ledger id: `run_20260531_072650_cc832d`
  - Verification command: `npm test`
  - Outcome: `complete`; verification `passed`
  - Redacted notes: isolated temporary prefix에 `codexus@next`
    (`0.1.0-alpha.5`)를 설치하고 throwaway git repository에서 실행했습니다.
    `codexus-e2e.txt`에 `CODX-E2E-OK`가 생성됐고 npm test script가 통과했습니다.
    Raw prompt, transcript, local temp path는 의도적으로 기록하지 않습니다.
- Codex CLI/TUI 안의 `$codexus` skill:
  - 날짜: 2026-05-31T07:29Z
  - 실행한 command: `session status --json`, `session checkpoint`,
    `session verify --verify "node -e \"process.exit(0)\""`
  - Outcome: session status `stability: stable`, checkpoint
    `checkpoint_20260531_072941_c41779`, session verification `passed`
  - Redacted notes: 현재 Codex session의 Codexus skill wrapper를 통해 실행했습니다.
    Raw transcript는 커밋하지 않습니다.
- 실제 `resume` / `cancel`:
  - 날짜: 2026-05-31T07:27Z-2026-05-31T07:29Z
  - Ledger id: original `run_20260531_072650_cc832d`; follow-up
    `run_20260531_072721_9c6502`
  - Outcome: resume이 실제 `codex-exec` follow-up run을 생성했고, live
    `cancel` 요청이 수락되어 follow-up이 terminal `cancelled`에 도달했습니다.
  - Redacted notes: cancellation ledger에는 `run.cancel_requested`,
    `driver.cancel_requested`, exit code 130의 `driver.failed`, terminal
    `run.terminal` outcome `cancelled`가 기록됐습니다. 실제 Codex-owned run의 live
    cancellation은 증명됐습니다. 더 엄격한 sign-off를 원한다면 final stable tag 전에
    successful real resume completion을 한 번 더 반복할 가치가 있습니다.
- Notify hook `turn-ended` 관측:
  - 날짜: 2026-05-31T05:39:58.684Z
  - `capabilities.hooks` transition: 실제 `turn-ended` dispatch 관측; runtime
    surface는 `cli-tui`
  - Redacted notes: session status는 `notifyDispatch.status: "observed"`를
    보고합니다.

## Release Decision

- Stable tag: pending
- npm version: prerelease rehearsal fallback으로 `0.1.0-alpha.5` publish
- `latest` dist-tag: `0.1.0-alpha.5`
- `next` dist-tag: `0.1.0-alpha.5`
- deferred surface가 docs/output에서 정직하게 표시됨: README support matrix,
  implementation/remaining-work 문서에서 확인
- Stable blocker: `v0.1.0` 전
  `raeseoklee/codexus/.github/workflows/release.yml` npm trusted publisher 설정
  또는 동등한 npm 2FA trust 작업 완료 필요
