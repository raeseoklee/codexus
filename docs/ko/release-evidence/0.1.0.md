# 0.1.0 Release Evidence

[English](../../release-evidence/0.1.0.md)

상태: pending

이 파일은 `0.1.0` stable cut을 위한 redacted manual sign-off artifact입니다. Raw run
ledger, transcript, stdout dump, token, private project를 드러내는 local path, model
prompt는 커밋하지 않습니다. Release checklist를 증명하는 데 필요한 bounded evidence만
기록합니다.

## 자동 증거

- `npm run release:check`: pending
- `npm run package:smoke`: pending
- `cx supply-chain check --gate --json`: pending
- release commit의 GitHub CI: pending
- trusted-publishing prerelease rehearsal: pending

## 수동 E2E Sign-Off

- throwaway fixture에서 실제 `codex-exec` run:
  - 날짜:
  - Ledger id:
  - Verification command:
  - Outcome:
  - Redacted notes:
- Codex CLI/TUI 안의 `$codexus` skill:
  - 날짜:
  - 실행한 command:
  - Outcome:
  - Redacted notes:
- 실제 `resume` / `cancel`:
  - 날짜:
  - Ledger id:
  - Outcome:
  - Redacted notes:
- Notify hook `turn-ended` 관측:
  - 날짜:
  - `capabilities.hooks` transition:
  - Redacted notes:

## Release Decision

- Stable tag: pending
- npm version: pending
- `latest` dist-tag: pending
- `next` dist-tag: pending
- deferred surface가 docs/output에서 정직하게 표시됨: pending
