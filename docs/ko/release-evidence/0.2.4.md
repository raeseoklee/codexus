# 0.2.4 Release Evidence

[English](../../release-evidence/0.2.4.md)

Status: `0.2.4` prepared; post-publish live sign-off pending

이 파일은 구조화된 npm `next` dist-tag action reporting과 documentation baseline
cleanup을 위한 redacted release-prep evidence artifact입니다. Raw run ledger,
transcript, stdout dump, token, private project local path, model prompt를 커밋하지
마세요. Release checklist를 증명하는 데 필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.2.4`
  - Planned tag: `v0.2.4`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- `0.2.3` 이후 release scope:
  - `release check --live --json`은 이제
    `releaseIntegrity.npm.nextDistTagAction`을 보고합니다.
  - 새 action summary는 npm `next >= latest`이면 `satisfied`, `next`가 signed-off
    stable version보다 뒤처지면 정확한 maintainer command와 함께 `required`,
    `latest` 자체가 아직 release와 일치하지 않으면 `unknown`입니다.
  - Feature reference, public release checklist, JSON contract, remaining-work
    backlog, roadmap kanban, project wiki current-state, changelog, release
    evidence 문서를 tag publish 전에 갱신했습니다.

## Manual E2E Sign-Off

`0.2.4` tag publish 전 real-model E2E는 요구하지 않습니다. 이 release는 구조화된
release-integrity reporting과 documentation baseline cleanup만 추가합니다. Live model
execution, automatic installation mutation, automatic prompt injection, app health
authority, cleanup authority, 새로운 completion authority는 추가하지 않습니다.

## Deferred Surfaces

다음 항목은 stable contract 밖에 남아 있습니다:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, lease, retry;
- LSP protocol-server lifecycle과 automatic project LSP application;
- Desktop app-server live attachment;
- automatic context 또는 prompt injection;
- app health authority와 automatic cleanup.

## Post-Publish Evidence

`v0.2.4` publish와 live sign-off 실행 전까지 pending입니다:

- Release workflow run: pending
- npm registry:
  - Published version: pending
  - `latest` dist-tag: pending
  - `next` dist-tag: pending
  - Package shasum: pending
  - Package integrity: pending
  - Provenance: pending
- GitHub Release:
  - URL: pending
  - Release asset: `install.sh` pending
  - `install.sh` SHA-256: pending
- Live release gate:
  - Command:
    `node src/cli/main.ts release check --version 0.2.4 --live --gate --json`
  - Result: pending
  - Evidence gaps: pending
- Installed package smoke: pending
- GitHub Release installer smoke: pending
