# 0.2.7 Release Evidence

[English](../../release-evidence/0.2.7.md)

Status: `0.2.7` release prep complete; post-publish live sign-off pending

이 파일은 installer help-safety release-integrity gate를 위한 redacted release-prep
evidence artifact입니다. Raw run ledger, transcript, stdout dump, token, private
project local path, model prompt를 커밋하지 마세요. Release checklist를 증명하는 데
필요한 bounded evidence만 기록합니다.

## Automated Evidence

- Release prep:
  - Package version: `0.2.7`
  - Planned tag: `v0.2.7`
  - Publish path: tag-triggered GitHub Actions trusted publishing
- `0.2.6` 이후 release scope:
  - `release check --gate`는 `install.sh`가 Node/npm probe, `npm install`,
    bin-link mutation, Codex skill adapter 설치보다 먼저 `-h`/`--help`와 알 수
    없는 option을 처리할 때만
    `releaseIntegrity.installScript.helpNonDestructive:true`를 보고합니다.
  - 비파괴 help 처리를 증명하지 못하는 installer가 publish 전에 release gate에서
    실패함을 회귀 테스트로 고정했습니다.
  - 문서는 installer help safety를 local release integrity와 release checklist의
    일부로 명시합니다.

## Manual E2E Sign-Off

`0.2.7` tag publish 전 real-model E2E는 요구하지 않습니다. 이 release는
release-integrity hardening slice이며 live model execution, app health authority,
cleanup authority, prompt injection, 새로운 completion authority를 추가하지 않습니다.

## Deferred Surfaces

다음 항목은 stable contract 밖에 남아 있습니다:

- live `cx autopilot run`;
- unattended cron/gateway scheduler ownership, lease, retry;
- LSP protocol-server lifecycle과 automatic project LSP application;
- Desktop app-server live attachment;
- automatic context 또는 prompt injection;
- app health authority와 automatic cleanup.

## Post-Publish Evidence

Tag publish와 live sign-off 후 갱신 예정입니다.
